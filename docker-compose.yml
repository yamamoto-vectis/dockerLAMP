dockerlamp:
  build: ./LAMP
  dockerfile: 'Dockerfile'
  volumes:
    - ./:/var/www/html
githook_installer:
  build: ./hookinstaller
  dockerfile: 'Dockerfile'
  volumes:
    - ./hookinstaller/hooks:/tmp/hooks
    - ./.git/hooks:/tmp/proj  
